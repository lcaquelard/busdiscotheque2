{% extends 'base.html.twig' %}
{% set title = "Nos middle bus" %}
{% set meta_description = "Découvrez nos bus de taille moyenne en location !"%}
    {% block main %}
        {% for bus in buses %}
        <section id="{{ bus.getShortName() }}" class="bus rev">
            <div class="front">
                <div class="content">
                    <h3>{{ bus.getName() }}</h3>
                    <div class="bus-icon capacity">{{ bus.getRoom() }} max</div>
                    {% for name, option in bus.getOptions() %}
                        <div class="bus-icon {{ name }}">{{ option }}</div>
                    {% endfor %}
                </div>
            </div>
            <div class="back">
                <div class="content">
                    <h3>{{ bus.getName() }}</h3>
                    <div class="zoom"></div>
                </div>
            </div>
        </section>
        {% endfor %}
    {% endblock %}
{% block modals %}
    {% for bus in buses %}
        <div class="modal {{ bus.getShortName }}" role="dialog" aria-modal="true" aria-label="Galerie de photos du {{ bus.getShortName()}} agrandies">
            <div class="close"></div>
            <div class="slider">
                {% for i in 1..bus.getPictures() %}
                    <img src="{{ asset('build/images/photo/bus/middle/' ~ bus.getShortName() ~ '/' ~ i ~ '.webp') }}" alt="Image défilant horizontalement">
                {% endfor %}
            </div>
        </div>
    {% endfor %}
{% endblock %}